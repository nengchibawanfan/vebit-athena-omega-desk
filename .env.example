# 后端接口根路径。开发时走 Vite 代理，填 /api 即可
VITE_API_BASE_URL=/api

# true：使用本地 mock，不请求后端；false：请求真实接口
VITE_USE_MOCK=true

# 开发代理目标，对应真实后端地址
VITE_API_PROXY_TARGET=http://localhost:8080

# 挂单明细 WebSocket。留空则走当前站点 /ws/orders?pair=
# 例：ws://localhost:8080/ws
VITE_WS_BASE_URL=
